# Repository guidance

- The installable skill is `skills/repo-scaffolder/`; edit its `SKILL.md` and directly linked references for behavior changes.
- Keep examples conditional and consistent with the rule to avoid unused scaffolding.
- Verify local links, metadata, and syntax with `python scripts/validate_skill.py` after edits.
- This repository is the skill source, not an application to scaffold by default.
